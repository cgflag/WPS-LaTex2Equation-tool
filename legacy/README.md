# Legacy VBA macro (optional)

`LaTeXToEquation.bas` 为早期在 **Word/WPS 内用宏** 转换的方案；功能与 Python 版类似，但依赖 WPS/Word 的 `OMaths` 接口，兼容性因版本而异。

**推荐日常使用 Python 版**：`convert_latex_docx.py`（无需启用宏、可批处理）。

详见 `使用说明.txt`（VBA 安装与诊断步骤）。
